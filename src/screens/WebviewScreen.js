import React from 'react';
import WebView from 'react-native-webview';

const WebviewScreen = () => {
  return <WebView source={{uri: 'http://64.227.162.41/'}} style={{flex: 1}} />;
};

export default WebviewScreen;
